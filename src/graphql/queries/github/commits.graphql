query Commits($owner: String!, $name: String!, $branch: String!, $commits: Int!) {
  repository(owner: $owner, name: $name) {
    url
    ref(qualifiedName: $branch) {
      name
      target {
        __typename # no idea why this needs to exist, but oh well
        ... on Commit {
          history(first: $commits) {
            totalCount
            edges {
              node {
                messageHeadline
                oid
                url
              }
            }
          }
        }
      }
    }
  }
}
